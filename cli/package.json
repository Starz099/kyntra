{
  "name": "kyntra",
  "bin": {
    "kyntra": "./dist/index.js"
  },
  "files": [
    "dist",
    "public"
  ],
  "version": "1.1.0",
  "description": "",
  "main": "./dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "node -e \"require('node:fs').rmSync('dist',{ recursive: true, force: true })\"",
    "build": "pnpm run clean && tsc",
    "build:web": "pnpm --dir ../web run build",
    "sync:web": "node ./scripts/sync-web-dist.mjs",
    "build:package": "pnpm run build:web && pnpm run sync:web && pnpm run build",
    "prepublishOnly": "pnpm run build:package",
    "start": "node dist/index.js",
    "dev": "pnpm run build && pnpm run start"
  },
  "keywords": [],
  "author": "",
  "type": "module",
  "license": "ISC",
  "packageManager": "pnpm@10.30.3",
  "dependencies": {
    "@clack/prompts": "^1.0.1",
    "@fastify/static": "^9.0.0",
    "@google/genai": "^1.43.0",
    "commander": "^14.0.3",
    "fastify": "^5.7.4",
    "open": "^11.0.0",
    "repomix": "^1.12.0"
  },
  "devDependencies": {
    "@types/node": "^25.3.3",
    "typescript": "^5.9.3"
  }
}
